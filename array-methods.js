/*
1. Write a function called arrayCopier that that takes in an array and any validJS
datatype (ie. String, Number, Array, etc) and returns and array with the new item added to the end.

This function should return a COPY of the original array and NOT the original itself!

To get the most out of this exercise, try to use 2 or more array methods in your implementation.
*/

// Example
/***
 *
 * var arr1 = ['1', '2', 3, ['innerArrayItem', [2, 3]], 'Last Item'];
 * arrayCopier(arr1, 'newLastItem') -> would return ['1', '2', 3, ['innerArrayItem', [2, 3]], 'Last Item', 'newLastItem']
 *
 */
